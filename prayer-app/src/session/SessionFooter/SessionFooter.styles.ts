// New file generated from SessionFooter.tsx
import { cva } from "class-variance-authority";

export const sessionFooterVariants = cva(
    [
        "flex",
        "flex-wrap",
        "items-center",
        "gap-4",
        "border-t",
        "bg-card",
        "px-6",
        "py-4",
    ].join(" ")
);

export const sessionFooterLeadingVariants = cva(
    [
        "flex",
        "items-center",
        "gap-3",
    ].join(" ")
);

export const sessionFooterMetadataVariants = cva(
    [
        "flex",
        "flex-1",
        "flex-wrap",
        "items-center",
        "gap-4",
    ].join(" ")
);

export const sessionFooterActionsVariants = cva(
    [
        "flex",
        "items-center",
        "gap-3",
    ].join(" ")
);

export const sessionFooterTrailingVariants = cva(
    [
        "ml-auto",
        "flex",
        "items-center",
        "gap-2",
    ].join(" ")
);