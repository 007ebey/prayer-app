// New file generated from SessionInfo.tsx
import { cva } from "class-variance-authority";

export const sessionInfoVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-3",
    ].join(" ")
);

export const sessionInfoHeadingVariants = cva(
    [
        "flex",
        "items-center",
        "gap-3",
        "flex-wrap",
    ].join(" ")
);

export const sessionInfoDescriptionVariants = cva(
    [
        "text-muted-foreground",
    ].join(" ")
);

export const sessionInfoMetadataVariants = cva(
    [
        "flex",
        "flex-wrap",
        "items-center",
        "gap-3",
    ].join(" ")
);