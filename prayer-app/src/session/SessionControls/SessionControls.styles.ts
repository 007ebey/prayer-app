// New file generated from SessionControls.tsx
import { cva } from "class-variance-authority";

export const sessionControlsVariants = cva(
    [
        "flex",
        "items-center",
        "justify-between",
        "gap-4",
        "flex-wrap",
        "rounded-xl",
        "border",
        "bg-card",
        "p-4",
    ].join(" ")
);

export const sessionControlsLeadingVariants = cva(
    [
        "flex",
        "items-center",
        "gap-2",
    ].join(" ")
);

export const sessionControlsPrimaryVariants = cva(
    [
        "flex",
        "items-center",
        "gap-3",
        "flex-wrap",
    ].join(" ")
);

export const sessionControlsSecondaryVariants = cva(
    [
        "flex",
        "items-center",
        "gap-2",
        "flex-wrap",
    ].join(" ")
);

export const sessionControlsTrailingVariants = cva(
    [
        "flex",
        "items-center",
        "gap-2",
        "ml-auto",
    ].join(" ")
);