// New file generated from MemberCard.tsx
import { cva } from "class-variance-authority";

export const memberCardVariants = cva(
    [
        "rounded-xl",
        "border",
        "bg-card",
        "transition-all",
        "hover:-translate-y-1",
        "hover:shadow-lg",
    ].join(" ")
);