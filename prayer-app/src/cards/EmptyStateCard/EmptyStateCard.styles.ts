// New file generated from EmptyStateCard.tsx
import { cva } from "class-variance-authority";

export const emptyStateCardVariants = cva(
    [
        "rounded-xl",
        "border",
        "bg-card",
        "p-8",
        "text-center",
    ].join(" ")
);