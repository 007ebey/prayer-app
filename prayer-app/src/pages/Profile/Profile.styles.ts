// New file generated from Profile.tsx
import { cva } from "class-variance-authority";

export const profileVariants = cva(
    [
        "mx-auto",
        "max-w-7xl",
        "space-y-6",
        "px-6",
        "py-8",
    ].join(" ")
);

export const profileGridVariants = cva(
    [
        "grid",
        "gap-6",
        "lg:grid-cols-[320px_1fr]",
    ].join(" ")
);