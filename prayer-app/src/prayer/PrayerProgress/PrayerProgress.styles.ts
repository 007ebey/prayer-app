// New file generated from PrayerProgress.tsx
import { cva } from "class-variance-authority";

export const prayerProgressVariants = cva(
    [
        "rounded-xl",
        "border",
        "bg-card",
        "shadow-sm",
        "overflow-hidden",
    ].join(" ")
);

export const prayerProgressHeaderVariants = cva(
    [
        "px-5",
        "pt-5",
        "space-y-1",
    ].join(" ")
);

export const prayerProgressContentVariants = cva(
    [
        "px-5",
        "py-4",
        "space-y-4",
    ].join(" ")
);

export const prayerProgressFooterVariants = cva(
    [
        "border-t",
        "px-5",
        "py-4",
    ].join(" ")
);