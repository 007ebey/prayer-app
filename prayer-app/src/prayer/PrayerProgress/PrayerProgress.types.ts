// New file generated from PrayerProgress.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerProgressProps
    extends HTMLAttributes<HTMLElement> {

    heading?: ReactNode;

    description?: ReactNode;

    progress?: ReactNode;

    details?: ReactNode;

    actions?: ReactNode;

}