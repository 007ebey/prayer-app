// New file generated from PrayerHostPanel.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerHostPanelProps
    extends HTMLAttributes<HTMLElement> {

    header?: ReactNode;

    sessionControls?: ReactNode;

    agendaControls?: ReactNode;

    participantControls?: ReactNode;

    mediaControls?: ReactNode;

    quickActions?: ReactNode;

    footer?: ReactNode;

}