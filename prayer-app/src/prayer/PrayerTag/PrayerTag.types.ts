// New file generated from PrayerTag.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerTagProps
    extends Omit<
        HTMLAttributes<HTMLSpanElement>,
        "children"
    > {

    icon?: ReactNode;

    label?: ReactNode;

}