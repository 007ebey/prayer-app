// New file generated from PrayerTag.tsx
import { cva } from "class-variance-authority";

export const prayerTagVariants = cva(
    [
        "inline-flex",
        "items-center",
        "gap-2",
    ].join(" ")
);