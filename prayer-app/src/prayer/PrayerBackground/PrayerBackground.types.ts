// New file generated from PrayerBackground.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerBackgroundProps
    extends HTMLAttributes<HTMLDivElement> {

    image?: ReactNode;

    video?: ReactNode;

    overlay?: ReactNode;

    children?: ReactNode;

}