// New file generated from PrayerList.tsx
import { cva } from "class-variance-authority";

export const prayerListVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-6",
    ].join(" ")
);

export const prayerListHeaderVariants = cva(
    [
        "flex",
        "items-center",
        "justify-between",
        "gap-4",
    ].join(" ")
);

export const prayerListContentVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-4",
    ].join(" ")
);

export const prayerListFooterVariants = cva(
    [
        "flex",
        "justify-center",
        "pt-2",
    ].join(" ")
);