// New file generated from PrayerCategory.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerCategoryProps
    extends HTMLAttributes<HTMLElement> {

    heading?: ReactNode;

    description?: ReactNode;

    icon?: ReactNode;

    actions?: ReactNode;

    children?: ReactNode;

}