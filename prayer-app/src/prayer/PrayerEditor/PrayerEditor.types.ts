// New file generated from PrayerEditor.tsx
import {
    FormHTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerEditorProps
    extends FormHTMLAttributes<HTMLFormElement> {

    header?: ReactNode;

    toolbar?: ReactNode;

    titleField?: ReactNode;

    categoryField?: ReactNode;

    scriptureField?: ReactNode;

    contentField?: ReactNode;

    mediaField?: ReactNode;

    attachmentsField?: ReactNode;

    tagsField?: ReactNode;

    footer?: ReactNode;

}