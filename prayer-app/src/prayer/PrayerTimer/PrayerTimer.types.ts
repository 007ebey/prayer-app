// New file generated from PrayerTimer.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerTimerProps
    extends HTMLAttributes<HTMLElement> {

    heading?: ReactNode;

    time?: ReactNode;

    status?: ReactNode;

    actions?: ReactNode;

}