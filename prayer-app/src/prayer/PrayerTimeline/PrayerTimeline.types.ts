// New file generated from PrayerTimeline.tsx
import {
    HTMLAttributes,
    ReactNode,
} from "react";

export interface PrayerTimelineProps
    extends HTMLAttributes<HTMLElement> {

    header?: ReactNode;

    children?: ReactNode;

    footer?: ReactNode;

}