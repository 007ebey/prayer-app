// New file generated from PrayerParticipantList.tsx
import { cva } from "class-variance-authority";

export const prayerParticipantListVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-6",
    ].join(" ")
);

export const prayerParticipantListHeaderVariants = cva(
    [
        "flex",
        "items-center",
        "justify-between",
        "gap-4",
    ].join(" ")
);

export const prayerParticipantListContentVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-3",
    ].join(" ")
);

export const prayerParticipantListFooterVariants = cva(
    [
        "flex",
        "justify-center",
        "pt-2",
    ].join(" ")
);