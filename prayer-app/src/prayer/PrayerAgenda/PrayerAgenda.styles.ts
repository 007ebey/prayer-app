// New file generated from PrayerAgenda.tsx
import { cva } from "class-variance-authority";

export const prayerAgendaVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-6",
        "rounded-xl",
        "border",
        "bg-card",
        "p-6",
        "shadow-sm",
    ].join(" ")
);

export const prayerAgendaContentVariants = cva(
    [
        "flex",
        "flex-col",
        "gap-3",
    ].join(" ")
);