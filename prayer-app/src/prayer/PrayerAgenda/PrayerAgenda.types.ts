// New file generated from PrayerAgenda.tsx
import { ReactNode } from "react";

export interface PrayerAgendaProps {

    header?: ReactNode;

    items?: ReactNode;

    currentItem?: ReactNode;

    controls?: ReactNode;

    footer?: ReactNode;

}