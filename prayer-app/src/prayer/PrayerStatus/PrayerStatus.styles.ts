// New file generated from PrayerStatus.tsx
import { cva } from "class-variance-authority";

export const prayerStatusVariants = cva(
    [
        "inline-flex",
        "items-center",
        "gap-2",
    ].join(" ")
);