// New file generated from StatisticCard.tsx
import { cva } from "class-variance-authority";

export const statisticsCardVariants = cva(
    [
        "rounded-xl",
        "border",
        "bg-card",
        "transition-all",
        "hover:shadow-md",
    ].join(" ")
);