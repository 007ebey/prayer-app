// New file generated from ShareDialog.tsx
import { cva } from "class-variance-authority";

export const shareDialogVariants = cva(
    "w-full max-w-lg"
);

export const shareGridVariants = cva(
    "grid grid-cols-3 gap-4"
);