// New file generated from Modal.tsx
import Dialog from "../../../primitives/Dialog";

import type {
    ModalProps,
} from "./Modal.types";

const Modal = (
    props: ModalProps
) => {

    return (
        <Dialog
            {...props}
        />
    );

};

export default Modal;